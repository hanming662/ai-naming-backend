server:
  port: 8000

spring:
  datasource:
    url: jdbc:mysql://116.204.135.42:3306/ai_naming?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai&createDatabaseIfNotExist=true
    username: root
    password: root123321
    driver-class-name: com.mysql.cj.jdbc.Driver
    # Druid 连接池配置
    type: com.alibaba.druid.pool.DruidDataSource
    druid:
      initial-size: 5
      min-idle: 5
      max-active: 20
      max-wait: 60000
  jackson:
    property-naming-strategy: SNAKE_CASE

# MyBatis-Plus 配置
mybatis-plus:
  configuration:
    # 驼峰转下划线
    map-underscore-to-camel-case: true
    # 控制台打印 SQL（开发阶段开启）
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  global-config:
    db-config:
      # 主键策略：自增
      id-type: auto
      # 逻辑删除（可选）
      # logic-delete-field: deleted
      # logic-delete-value: 1
      # logic-not-delete-value: 0
    banner: false

# AI API 配置
ai:
  api-key: sk-1ab30c320c7f4453a7f5b352c4afe29c
  api-url: https://api.deepseek.com/chat/completions
  model: deepseek-chat

# 应用配置
app:
  free-generate-times: 3